mixin card(img, title, desc, button)
  .card.default-card
    figure
      img(src=img alt='img')/
    .text
      h6.title #{title} 
      p #{desc}
    |
    |
    a.btn.waves-effect.block.secondary.button(href="#") #{button}
    |
    |
